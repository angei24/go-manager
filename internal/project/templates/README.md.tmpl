# {{.Module}}

Go project managed with [gm](https://github.com/angei24/go-manager).

## Commands

```bash
gm sync              # tidy and download dependencies
gm add <package>     # add a dependency
gm remove <package>  # remove a dependency
```

Go version is pinned in `.gm-version`.
